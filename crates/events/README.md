# `forgeerp-events`

**Responsibility:** Domain events and integration events.

## Boundaries
- Defines event types and contracts.
- May depend on `core` (to reference domain identifiers/value objects).
- Must not depend on `api` or `infra`.


