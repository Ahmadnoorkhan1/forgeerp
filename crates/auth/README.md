# `forgeerp-auth`

**Responsibility:** Users, roles, permissions, and JWT logic.

## Boundaries
- Defines auth domain types and policy interfaces.
- May depend on `core`.
- Must not depend on `api` or `infra`.


